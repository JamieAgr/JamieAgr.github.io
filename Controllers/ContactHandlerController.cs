using Microsoft.AspNetCore.Mvc;
using ShowcaseWebdev.Models;

namespace ShowcaseWebdev.Controllers
{
    public class ContactHandlerController : Controller
    {
        /*[HttpPost]
        public ActionResult HandleFormSubmission(Mail mail)
        {
            Console.Write("werkt");
            var contactHandler = new ContactHandler();
            bool success = contactHandler.HandleFormSubmission(mail);
            if (success)
            {
                return Content("Email sent successfully");
            }
            else
            {
                return Content("Failed to send email");
            }
        }*/
    }
}
